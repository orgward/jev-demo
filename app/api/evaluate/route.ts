import {NextRequest,NextResponse} from "next/server";import {evaluate} from "../../../lib/jev";
export const runtime="nodejs";export async function POST(req:NextRequest){try{if(!process.env.TYPESAFE_API_KEY)return NextResponse.json({error:"TYPESAFE_API_KEY is not configured in this runtime."},{status:503});const body=await req.json();if(!body?.state||!Array.isArray(body.questions)||!body.questions.length)return NextResponse.json({error:"state and questions are required"},{status:400});const started=Date.now();const result:any=await evaluate(body.state,body.questions);return NextResponse.json({result,latencyMs:Date.now()-started});}catch(e:any){return NextResponse.json({error:e?.message||"Jev request failed"},{status:500});}}